{
  "name": "jessie",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "tsc && vite build && aws s3 sync ./dist s3://jesseleung.com"
  },
  "dependencies": {
    "preact": "^10.9.0",
    "wouter": "^2.8.0-alpha.2"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.3.0",
    "typescript": "^4.6.4",
    "vite": "^3.0.0"
  }
}
